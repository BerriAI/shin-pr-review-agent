# PostgreSQL — same DB as the Python bot or a separate one
DATABASE_URL=postgresql://user:pass@localhost:5432/litellm_bot

# LiteLLM proxy
LITELLM_API_BASE=http://localhost:4000
LITELLM_API_KEY=sk-...
LITELLM_DEFAULT_MODEL=anthropic/claude-sonnet-4-6

# Auth (both optional — disables auth gate if unset)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=secret
BOT_API_KEYS=key1,key2
SESSION_SECRET=some-random-string

# Security agent (LiteLLM Managed Agent) — optional; skipped if unset
SECURITY_AGENT_BASE=https://your-litellm-instance.example.com
SECURITY_AGENT_ID=your-agent-uuid-here

# GitHub token — no longer needed; GitHub API calls go through LiteLLM MCP
# GITHUB_TOKEN=ghp_...

PORT=8081
